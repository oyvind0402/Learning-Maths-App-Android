<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mappe1apputvikling_s188886_s344105">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/matte"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/matte_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mappe1ApputviklingS188886_s344105">
        <activity
            android:name=".FrontPageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GameActivity"
            android:exported="true"
            android:parentActivityName=".FrontPageActivity" />
        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:parentActivityName=".FrontPageActivity" />
        <activity
            android:name=".StatisticsActivity"
            android:exported="true"
            android:parentActivityName=".FrontPageActivity" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>